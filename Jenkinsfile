pipeline {
    agent any
    stages {
        stage('test') {
            steps {
                echo 'Testing the app'
            }
        }
        stage('build') {
            steps {
                echo 'Building the app'
            }
        }
        stage('deploy') {
            steps {
                echo 'Deploying the app'
            }
        }
    }
}
